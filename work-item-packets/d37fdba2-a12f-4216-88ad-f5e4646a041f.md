# Work Item Packet

## Work item id
d37fdba2-a12f-4216-88ad-f5e4646a041f

## Action
Send a platform invite to `nray@removeasap.com` for the ASAP Pest & Wildlife account.

**This action is blocked.** Two blockers apply simultaneously:

1. **Standing approval gate — client communication.** Sending any invite email or
   platform-access grant is an outbound communication touching a production system.
   The rig contract requires Robert approval before execution.

2. **Under-specified task.** The source request ("pls send invite to nray@removeasap.com
   as well") does not state which platform the invite is for. Candidates:
   - GoHighLevel (CRM/automation) — team member or sub-account user
   - Cloudflare Pages / dashboard — collaborator access
   - Google Workspace — org member or shared Drive
   - The BWM client portal / review page
   - Something else entirely

   Without knowing the platform, role, and permission level, no invite can be sent
   without guessing at client-facing behavior.

## Risk
- Granting access to the wrong platform or wrong permission tier exposes client
  data or production systems to an unintended party.
- Sending an unsolicited invite to an email the client did not confirm as active
  could be confusing or surfaced as noise.
- Low blast radius once the platform is confirmed; reversible (revoke access).

## Approval path
Robert must confirm **two things** before execution proceeds:

1. **Which platform?** (GHL, Cloudflare, Google Workspace, other — be specific)
2. **What role / permission level?** (admin, member, viewer, etc.)

Once confirmed, the invite action is a ~30-second manual step in the relevant
platform UI (not a code change in this repo). It should be executed by a human
with admin access to that platform, or routed via Bob gateway if the platform
supports programmatic invites.

## Verification evidence
No code change was made. This packet is the only artifact produced.

The task is under-specified and hits the standing client-communication gate —
per spec: "create the smallest useful prep packet instead of guessing at
client-facing behavior."

## Rollback/abort path
If the invite is sent and needs to be revoked: remove the user from the relevant
platform's team/member list. All platforms listed above support member removal
without data loss.
