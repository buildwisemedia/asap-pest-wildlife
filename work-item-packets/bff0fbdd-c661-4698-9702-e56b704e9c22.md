# Work Item: GTM phone_click + email_click Tracking

## Action
Verify and document completed phone_click + email_click GA4 event tracking implementation in attribution.js (v2.10, lines 258–322).

**What was implemented:**
- Delegated click listener on document (capture phase) for tel: and mailto: links
- GA4 events fired with privacy-redacted payloads:
  - `phone_click` → `{ phone_number_redacted: "<last 4 digits>", page_path, page_referrer }`
  - `email_click` → `{ email_domain: "<provider>", page_path, page_referrer }`
- Events pushed to dataLayer + fired via gtag()
- Transport type = 'beacon' for mobile safety (tel:/mailto: navigation survival)
- No preventDefault — native handlers still run; additive to existing GTM tracking

**Status:** JavaScript implementation verified complete. GTM tag configuration (in GTM UI) = owner's task.

## Risk
**Low.** Implementation is:
- Non-destructive: additive to existing click tracking, no preventDefault
- Privacy-forward: redacts phone to last 4 digits, email domain only
- Mobile-safe: uses beacon transport for unload safety
- Browser-compatible: vanilla JS, no external dependencies
- Backward-compatible: early return if window.gtag not available

**Residual risk:** GTM tag must be configured in GTM UI to capture these events; JavaScript alone only fires dataLayer + gtag() calls. Configuration is out of scope for this work item.

## Approval path
Robert must approve the work packet before the gated action completes. Once approved, the implementation can proceed to integration testing + deployment per client SOP.

## Verification evidence
1. **Source:** `attribution.js` lines 258–322 — phone_click + email_click delegated handler
2. **Test utility:** `assets/js/gtm-phone-click-test.js` — browser console test function
3. **Integration doc:** GTM-PHONE-CLICK-IMPLEMENTATION.md — implementation summary + GTM tag config checklist (documented in parallel delivery; not yet committed to repo)
4. **Event mapping:** GTM-TRACKING.md — comprehensive event taxonomy including phone_click + email_click
5. **Live test page:** gtm-phone-click-test.html — standalone test with real-time event log

**Manual verification steps:**
```bash
# 1. Verify implementation in attribution.js
grep -n "phone_click\|email_click" attribution.js

# 2. Load gtm-phone-click-test.html in browser
# 3. Click tel: and mailto: links
# 4. Check browser console: window.__gtmPhoneClickTest()
# 5. Verify events in GA4 real-time reporting
```

## Rollback/abort path
If GTM tag configuration fails or events are not firing in GA4:
1. Revert commit via git (no client-facing change)
2. Escalate to GTM administrator to verify container configuration
3. Re-test after GTM tag is correctly wired in Tag Manager

No data loss or state change — pure JavaScript implementation with no side effects.

## Work item id
`bff0fbdd-c661-4698-9702-e56b704e9c22`
